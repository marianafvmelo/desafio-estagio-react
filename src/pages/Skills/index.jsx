import { Registers } from "../../components/Registers";

export function Skills() {
  return (
    <Registers labelText="Habilidades:" inputId="skills" placeholderText="Digite uma habilidade.." />
  )
}